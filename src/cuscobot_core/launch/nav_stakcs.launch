<?xml version="1.0" ?>

<launch>
  <master auto="start"/>

  <!-- Arguments -->
  <arg name="map_file" default="$(find cuscobot_core)/maps/mapa260825.yaml"/>  

  <!-- Map server-->
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" />
  
  <!-- Run AMCL-->
  <include file="$(find cuscobot_core)/launch/amcl.launch" />

  <!-- Run move_base-->
  <node pkg="move_base" type="move_base" respawn="false" name="move_base" output="screen">
    <rosparam file="$(find cuscobot_core)/param/common_costmap.yaml" command="load" ns="global_costmap" />
    <rosparam file="$(find cuscobot_core)/param/common_costmap.yaml" command="load" ns="local_costmap" />
    <rosparam file="$(find cuscobot_core)/param/local_costmap.yaml" command="load" />
    <rosparam file="$(find cuscobot_core)/param/global_costmap.yaml" command="load" />
    <rosparam file="$(find cuscobot_core)/param/base_local_planner.yaml" command="load" />
    <rosparam file="$(find cuscobot_core)/param/move_base.yaml" command="load" />
  </node>

  <!-- rviz -->
  <node 
    pkg="rviz" 
    type="rviz" 
    name="rviz" 
    args="-d $(find cuscobot_core)/urdf/config.rviz"
    required="true"    

    />

</launch>